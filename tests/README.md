# Tests

Cross-module automated tests and shared test fixtures belong here. Focused tests may also live beside source modules when that improves ownership.

Tests should verify serializable data contracts, deterministic calculations, validation diagnostics, and official example behavior without relying on DCR Sales Hub.

