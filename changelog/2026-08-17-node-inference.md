# Professional node inference

- Added temporary horizontal, vertical, and 45-degree references from nearby construction nodes while sketching a Deck Boundary.
- Combined the active segment direction with a node projection so two useful relationships can resolve one precise point.
- Shows only the strongest guide, with a highlighted reference node, inferred intersection, and descriptive cursor feedback.
- Added separate persistent controls for edge, grid, node, and 45-degree inference behavior.
- Kept inference temporary: placing a point records exact geometry without silently creating a permanent constraint.
- Added larger touch tolerances and snap-release hysteresis for stable field use without trapping the pointer.
- Added automated coverage for axis, diagonal, preference, migration, and release-tolerance behavior.
